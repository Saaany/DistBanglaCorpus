# Reference aggregate outputs

These files are consolidated, text-free aggregate outputs from the reference paper run. Raw corpus text and the `top_dup_clusters` artifact are intentionally excluded.

Use these files to validate that a reproduction has the expected schema and approximate reported values. Exact runtimes may differ across cloud environments.
